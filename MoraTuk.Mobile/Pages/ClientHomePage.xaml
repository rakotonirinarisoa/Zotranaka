<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="MoraTuk.Mobile.Pages.ClientHomePage">

    <ScrollView x:Name="MainScrollView">

        <VerticalStackLayout
            Padding="20"
            Spacing="15">

            <!-- Header -->

            <Label
                Text="🛺 ZOTRANAKA"
                FontSize="30"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                TextColor="#00A86B"/>
             <Frame BackgroundColor="White" 
                CornerRadius="15" 
                Padding="0" 
                HasShadow="True" 
                HeightRequest="300">
                    <WebView x:Name="MapWebView"
                    VerticalOptions="Fill"
                    HorizontalOptions="Fill" />
            </Frame>
            <Label
                Text="Où voulez-vous aller ?"
                FontSize="18"
                FontAttributes="Bold"
                TextColor="#333"/>

            <!-- Départ -->

            <Frame
                BackgroundColor="White"
                CornerRadius="15"
                Padding="10"
                HasShadow="True">

                <HorizontalStackLayout>

                    <Label
                        Text="📍"
                        FontSize="20"/>

                    <Label
                        x:Name="DepartureLabel"
                        Text="Recherche de votre position..."
                        TextColor="#222222"
                        FontSize="16"
                        VerticalOptions="Center"/>

                </HorizontalStackLayout>

            </Frame>

            <!-- Destination -->

            <Frame
                BackgroundColor="White"
                CornerRadius="15"
                Padding="5"
                HasShadow="True">

                <Entry
                    x:Name="DestinationEntry"
                    Placeholder="🏁 Destination"
                    TextColor="#222222"
                    PlaceholderColor="#888888"
                    TextChanged="DestinationEntry_TextChanged"/>

            </Frame>

            <!-- Liste recherche -->

            <CollectionView
                x:Name="DestinationList"
                IsVisible="False"
                HeightRequest="200"
                BackgroundColor="White"
                SelectionMode="Single"
                SelectionChanged="Destination_Selected">

                <CollectionView.ItemTemplate>

                    <DataTemplate>

                        <Border
                            Stroke="#DDDDDD"
                            StrokeThickness="1"
                            BackgroundColor="White"
                            Padding="12">

                            <Label
                                Text="{Binding Name}"
                                FontSize="16"
                                TextColor="#222222"/>

                        </Border>

                    </DataTemplate>

                </CollectionView.ItemTemplate>

            </CollectionView>

            <!-- Type course -->

            <Label
                Text="Type de course"
                FontAttributes="Bold"
                TextColor="#333"/>

            <Frame
                BackgroundColor="White"
                CornerRadius="15"
                Padding="5"
                HasShadow="True">

                <Picker
                    x:Name="RideTypePicker"
                    Title="Choisir le type"
                    TextColor="#222222"
                    TitleColor="#888888">

                    <Picker.Items>

                        <x:String>Tuk-Tuk partagé</x:String>
                        <x:String>Tuk-Tuk complet</x:String>

                    </Picker.Items>

                </Picker>

            </Frame>

            <!-- Prix -->

            <Button
                Text="💰 Calculer le prix"
                HeightRequest="50"
                CornerRadius="25"
                BackgroundColor="#FFC107"
                TextColor="White"
                FontAttributes="Bold"
                Clicked="CalculatePrice_Clicked"/>

            <Frame
                BackgroundColor="White"
                CornerRadius="15"
                Padding="15"
                HasShadow="True">

                <Label
                    x:Name="PriceLabel"
                    Text="Prix estimé : -"
                    FontSize="18"
                    HorizontalOptions="Center"
                    TextColor="#00A86B"
                    FontAttributes="Bold"/>

            </Frame>

            <!-- Numéro MVola du client -->

            <Label
                Text="Numéro MVola"
                FontAttributes="Bold"
                TextColor="#333"/>

            <Frame
                BackgroundColor="White"
                CornerRadius="15"
                Padding="5"
                HasShadow="True">

                <Entry
                    x:Name="MvolaNumberEntry"
                    Placeholder="Ex : 0329208949"
                    Keyboard="Telephone"
                    MaxLength="10"
                    TextColor="#222222"
                    PlaceholderColor="#888888"/>
            </Frame>
            <!-- Recherche chauffeur -->

            <Button
                Text="🚕 Rechercher un chauffeur"
                HeightRequest="55"
                CornerRadius="25"
                BackgroundColor="#00A86B"
                TextColor="White"
                FontAttributes="Bold"
                Clicked="SearchDriver_Clicked"/>

            <!-- Statut -->

            <Frame
                BackgroundColor="White"
                CornerRadius="15"
                Padding="15"
                HasShadow="True">

                <Label
                    x:Name="StatusLabel"
                    Text="Statut : en attente"
                    FontSize="16"
                    HorizontalOptions="Center"
                    TextColor="#555"/>

            </Frame>
            <Frame
                x:Name="MvolaPaymentFrame"
                IsVisible="False"
                BackgroundColor="#FFF8E1"
                BorderColor="#FF9800"
                CornerRadius="15"
                Padding="20"
                HasShadow="True">

                <VerticalStackLayout
                    Spacing="10">

                    <Label
                        Text="🟡 Paiement MVola en attente"
                        FontSize="20"
                        FontAttributes="Bold"
                        TextColor="#E65100"
                        HorizontalOptions="Center"/>

                    <Label
                        Text="Une demande de paiement est en attente."
                        FontSize="16"
                        TextColor="#333333"
                        HorizontalOptions="Center"/>

                    <Label
                        Text="Veuillez valider votre paiement MVola."
                        FontSize="16"
                        FontAttributes="Bold"
                        TextColor="#333333"
                        HorizontalOptions="Center"/>

                </VerticalStackLayout>

            </Frame>
            
        </VerticalStackLayout>

    </ScrollView>

</ContentPage>
